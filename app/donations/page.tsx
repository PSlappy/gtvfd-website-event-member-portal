import ComingSoon from "@/components/jumbotron/ComingSoon";

export default function DonationsPage() {
  return (
    <ComingSoon
      title="Donations"
      description="Donation collection is coming once a payment method is decided. Amenities and donations are handled manually via Venmo for now."
    />
  );
}
